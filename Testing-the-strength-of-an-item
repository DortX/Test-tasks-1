print('Бросатор v 1.0')
print('Представьте, что Вы инженер, который изобрёл противоударный материал.')
print('Вам известно, при падении с какой высоты материал повреждается.')
print('Но экспериментатору Егору это неизвестно. Ему предстоит вычислить предельно допустимую высоту.')
print('Вы предоставили экспериментатору всего два образца материала.')
print('Егор пообещал, что используя два образца')
print('материала, он сможет вычислить предельную высоту максимум за 149 попыток.')
print('Вы удивились, потому решили понаблюдать за экспериментом...\n')

yes = 'да'
while yes == 'да' or yes == 'Да' or yes == 'yes' or yes == 'Yes' or yes == 'Y' or yes == 'y':
    x = 0 # Значение x используем для подсчёта количества экспериментов
    height = 0 # Объявляем переменную для заданной высоты
    step = 0 # Задаём переменную для первого объекта (метры с шагом в сто единиц)
    attemp = 0 # Задаём переменную для второго объекта (метры точные)
    while True:
        try:
            height = int(input('Вы знаете, что объект пострадает от удара при падении с высоты ... метров (введите число от 1 до 5000): '))
            if height > 0 and height <= 5000:
                print('Егор: Отлично! Я приступаю к ЭЭЭкспериментам!\n')
                break
            else:
                print('Егор: Вы наверное что-то путаете...')
                continue
        except ValueError:
            print('Егор: А вы точно инженер?')
            print('Егор: Попробуйте-ка ещё разок... Может в этот раз получится?')
            continue

    for step in range(100,5100,100):
        x += 1
        if height > step and step <= 4900:
            print('Егор: Эксперимент №' + str(x), 'Бросаю первый объект с высоты', step, 'метров.')
        elif height <= step and step <= 4900:
            print('Егор: Эксперимент №' + str(x), 'Первый образец разбился на высоте', step, ' метров... Что же, бросим второй!')
            attemp = step - 100
            break
        elif height == 4999:
            step = 4999
            print ('Егор: Эксперимент №' + str(x), '', step, ' метров...!')
            print('Егор: Первый образец разбит! Перейдём ко второму...')
            attemp = step - 99
            break
        elif height == 5000:
            step = 5000
            print ('Егор: Эксперимент №' + str(x), '', step - 1, ' метров...!')
            print('Егор: Образец уцелел на высоте', step - 1, 'метров!')
            attemp = step
            print('Егор: Хм... На пяти тысячах объект точно разобъётся!')
            break
        
    while x < 149:
        if height > attemp and attemp <= 4997:
            x += 1
            attemp += 1
            print('Егор: Эксперимент №' + str(x), '. Высота: ', attemp, 'метров. Бросаю второй объект.')
        elif height == attemp <= 4998:
            print('\nЕгор: Второй объект разбился на высоте', attemp, 'метров! Значит предельно допустимая высота составляет...')
            print(attemp - 1, 'метров! Я узнал это в ходе всего', x, 'экспериментов!')
            break
        elif height == 4999:
            print('\nЕгор: Ну, всё понятно! 4998 метров! Вот предельно допустимая высота для этого объекта!')
            print('Егор: Как мы помним, первый объект разбился на высоте 4999 метров.')
            break
        elif attemp == 5000:
            print('\nЕгор: Ну, всё понятно! 4999 метров! Вот предельно допустимая высота для этого объекта!')
            break
        else:
            print('\nЕгор: Ну, всё понятно! 4999 метров! Вот предельно допустимая высота для этого объекта!')
    yes = input('Желаете повторить эксперименты? Да/Нет: ')
